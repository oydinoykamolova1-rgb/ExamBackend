{
  "buildCommand": "cd Exam-Frontend && npm install && npm run build",
  "outputDirectory": "Exam-Frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
